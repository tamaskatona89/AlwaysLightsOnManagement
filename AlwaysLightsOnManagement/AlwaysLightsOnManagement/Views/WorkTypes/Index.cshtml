@model IEnumerable<AlwaysLightsOnManagement.WorkType>

@{
    ViewData["Title"] = "Index";
}

<h1>Javítások jellege (WorkTypes)</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WorkTypeDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkTypeDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorkTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorkTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorkTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
